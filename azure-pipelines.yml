# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml
trigger:
- main
pool:
  vmImage: ubuntu-latest
steps:
- pwsh: |
    $username = "yuganshvirsingh@gmail.com"
    $newPassword = "shubhisinghji"
    $password = ConvertTo-SecureString $newPassword -AsPlainText -Force
    $creds = new-object -typename System.Management.Automation.PSCredential -argumentlist $username, $password
    Send-MailMessage -To "yuganshvirsingh@gmail.com" -ReplyTo "yuganshvirsingh@gmail.com" -From "yuganshvirsingh@gmail.com" -SmtpServer "smtp.gmail.com" -Port 465 -Subject "Test" -Body "Hello world 2" -Credential $creds
